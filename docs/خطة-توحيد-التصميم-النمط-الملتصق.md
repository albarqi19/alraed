# خطة توحيد التصميم — «النمط الملتصق» (Workspace Design System)

> **الحالة (2026-07-16):** المراحل 0+1 ✅، الدفعات 1+2+3 ✅ كاملة، ومن دفعة 7: جداول المعلمين + الانتظار + الإشراف ✅، ومن دفعة 8: ملف المعلم ✅. إضافات خارج الخطة: تحسين القشرة (طيّ القائمة الجانبية لعمود أيقونات + ترويسة ثابتة + بطاقة مستخدم سفلية + تاريخ هجري بالهيدر) ✅، وإزالة الشريط الجانبي الملوّن من كل الصفحات (قرار المالك) ✅. **التالي: دفعة 4 — الطلاب.**
> ملاحظة مؤجلة: صفحة الانتظار فيها ملاحظات تصميمية سيعود لها المالك.
> **المبدأ الحاكم:** ثيم واحد نظيف من tokens، لا تمرير إلا في الأشياء التي فيها تمرير، كثافة برنامج حاسوبي، ولا يُكتب لون صلب في أي صفحة بعد اليوم.

---

## 1) ما أُنجز (المرحلة 0)

| الملف | الدور |
|---|---|
| `src/styles/workspace.css` | نظام التصميم كاملاً: tokens `--ws-*` (مشتقة من ثيمات النظام تلقائياً) + مكونات `ws-*` |
| `src/modules/admin/layouts/admin-shell.tsx` | قائمة `WORKSPACE_ROUTES`: أي مسار فيها تتثبت شاشته بلا تمرير خارجي (ديسكتوب فقط) |
| `src/modules/admin/pages/admin-attendance-page.tsx` | **التطبيق المرجعي** — أي سؤال «كيف أبني X؟» جوابه في هذه الصفحة |

---

## 2) الطريقة المثلى للبداية (التوصية)

### المرحلة 1 — طبقة مكونات React (قبل أي ترحيل جماعي) ⭐
نسخ الـ markup يدوياً في 60 صفحة = صيانة مستحيلة. نبني مرة واحدة مكونات خفيفة في `src/shared/workspace/`:

```
WsPage, WsHeader (title/badge/actions/facts), WsFact,
WsToolbar, WsField (label+input/select),
WsLayout, WsMain, WsSideCol (الطي + localStorage مدمجان),
WsBlock (head/tools/fill), WsTable (thead لاصق + صف محدد),
WsChip, WsBtn, WsAlert, WsEmpty, WsSpinner, WsModal, WsProgress
```

- مكونات **عرض فقط** (thin wrappers فوق كلاسات `ws-*`) — بلا منطق أعمال، بلا state إلا طيّ العمود.
- بعدها تُعاد كتابة صفحة الحضور بها (تنظيف المرجع نفسه) — فتصير أول صفحة «موثّقة بالكود».
- **مدة متوقعة:** جلسة واحدة. هذا الاستثمار يجعل كل صفحة لاحقة أسرع 3-4 مرات.

### المرحلة 2 فصاعداً — ترحيل بالدفعات (وصفة واحدة لكل دفعة)
كل دفعة = صفحات متشابهة البنية → تُبنى بنفس الوصفة → **معاينة واعتماد** → commit مستقل → الدفعة التالية. لا ننتقل لدفعة قبل اعتماد سابقتها.

---

## 3) الوصفات الخمس (Archetypes)

| الوصفة | البنية | متى تدخل `WORKSPACE_ROUTES`؟ |
|---|---|---|
| **A — سجل وتفاصيل** | ترويسة + فلاتر + جدول وسط + عمود تفاصيل قابل للطي | ✅ دائماً |
| **B — قائمة كبيرة** | ترويسة + فلاتر + جدول يملأ الشاشة (التفاصيل درج/مودال) | ✅ دائماً |
| **C — لوحة بلوكات** | ترويسة + شبكة بلوكات ملتصقة (إحصاء/ملفات شخصية) | ✅ غالباً |
| **D — شبكة زمنية** | ترويسة + شبكة جدول أسبوعي/حصص تملأ الشاشة | ✅ دائماً |
| **E — إعدادات ونماذج** | نفس الـ tokens والمكونات لكن **صفحة تتمرر طبيعياً** | ❌ لا تدخل |

> قاعدة الوصفة E هي تطبيق مبدأ «لا تمرير إلا في الأشياء التي فيها تمرير» بالعكس: النماذج الطويلة طبيعتها التمرير — نوحّد شكلها لا سلوكها.

---

## 4) خريطة الدفعات (بأولوية الاستخدام اليومي)

### دفعة 1 — المواظبة اليومية (وصفة A) — الأثر الأكبر ✅
- [x] `admin-approval-page` اعتماد التحضير (3 أعمدة + بانل الحصص المفقودة الحي)
- [x] `admin-late-arrivals-page` إدارة التأخير (+ حوار الإجراء الجماعي ws-pick)
- [x] `admin-leave-requests-page` طلبات الاستئذان (طباعة QR محفوظة)
- [x] `admin-absence-excuses-page` أعذار الغياب (التفاصيل عمود يسار + خط زمني للمراجعات)

### دفعة 2 — كشوف الحضور (وصفة B) ✅
- [x] `attendance-report-page` كشف الغياب (عمود بناء الكشف يمين + ws-matrix)
- [x] `admin-period-attendance-page` تحضير الحصص (3 أعمدة: تنبيهات/شبكة/تفاصيل خلية)
- [x] `admin-barcode-attendance-page` حضور البوابة (عمود البث الحي — WebSocket والأصوات سليمة)

### دفعة 3 — المعلمون (A + B) ✅
- [x] `admin-teachers-page` إدارة المعلمين (B)
- [x] `admin-teacher-attendance-page` حضور المعلمين (تبويبات seg + عمود «غير المرتبطين» + 6 مودالات ws)
- [x] `admin-delay-actions-page` إجراءات التأخير + كل معالجاتها (الأعذار عمود حي يسار)
- [x] `admin-remote-attendance-page` الدوام عن بعد (3 أعمدة)

### دفعة 4 — الطلاب (A + C) ✅
- [x] `admin-students-page` إدارة الطلاب (عمود مستكشف الصفوف والشعب بالأعداد + ترقيم مثبت)
- [x] `admin-student-profile-page` ملف الطالب (3 أعمدة: قائمة/تبويبات/بطاقة هوية+فترة — «إيقاع الحضور» كمربعات ملونة)
- [x] `admin-class-sessions-page` إدارة الحصص (أُلغي مودال اليوم — الأيام عمود يميني والحصص جدول وسط + فلاتر toolbar)
- [x] `admin-evaluation-settings-page` تقييم الطلاب (لوحة نبض اليوم: أنماط يمين/مخطط+آخر التقييمات وسط/أكثر الفصول يسار — أُزيل framer-motion)

### دفعة 5 — الرسائل والتواصل (A) ✅
- [x] `whatsapp-hub-page` مركز الواتساب (عمود «القناة» بنبض الاتصال + أزرار طوارئ الانتظار + 3 تبويبات، TanStack table محفوظ)
- [x] `whatsapp-send-page` الإرسال (قمرة ثلاثية: جمهور/رسالة/**معاينة فقاعة واتساب حية مخصصة لأول محدد**)
- [x] `whatsapp-templates-page` القوالب (استوديو مقيم بلا مودال + معاينة تستبدل المتغيرات بأمثلتها)
- [x] `admin-teacher-messages-page` رسائل المعلمين (قاطع النظام بالترويسة + نبض القناة يمين + حدود خاصة يسار)
- [x] `admin-parent-replies-page` ردود أولياء الأمور (وارد بريدي + **التفاصيل كمحادثة واتساب**: رسالتنا خضراء وردّهم وارد)
- [x] `admin-sms-gateway-page` بوابة SMS (الأجهزة عمود دائم بنبض النشاط + تبويبا السجل والإرسال بعداد 160)

### دفعة 6 — التوجيه والسلوك والإحالات (A + C)
- [ ] `admin-behavior-page` + `admin-behavior-detail-page` + `admin-behavior-analytics-page`
- [ ] `admin-referrals-page` + `admin-referral-detail-page`
- [x] `student-cases-page` + `admin-student-case-details-page` + `admin-student-case-form-page` (لوحة الألوان الهادئة الموحدة في `student-cases-ui.tsx` + **مقياس شدة الأولوية** كأعمدة متصاعدة + تايم لاين إجراءات بأيقونة ولون لكل نوع + شرائح توقيت المتابعات «متأخرة/اليوم/بعد X يوم» + نموذج بخطوات مرقمة و**معاينة حية** لبطاقة الحالة + إصلاح مسار `/edit` الذي كان 404)
- [ ] `treatment-plans-page` + تفاصيلها ونموذجها
- [ ] `admin-parent-meeting-page`, `admin-summons-page`

### دفعة 7 — الجداول الزمنية (وصفة D — الأصعب تقنياً) — منجزة جزئياً
- [x] `admin-schedules-page` الخطط الزمنية (111KB — قائمة يمين + تفاصيل وسط مع «**مسطرة اليوم**»: شريط بصري بعرض مدد الفترات الحقيقية بألوان الأنواع، في التفاصيل ومعاينة التوليد السريع؛ 4 مودالات ws؛ منطق التوليد السريع ~450 سطراً محفوظ حرفياً)
- [x] `admin-teacher-schedules-page` جداول المعلمين (سحب وإفلات + ألوان مواد + 3 نوافذ ws)
- [x] `admin-class-schedules-page` جداول الفصول (نفس لوحة ألوان المواد + ws-matrix + معالجا الحذف متعدد الخطوات كمكوّن DeletionWizard موحد)
- [x] `admin-teacher-standby-page` جدول الانتظار (4 تبويبات + مصفوفة أسبوعية) — ⚠️ ملاحظات تصميمية مؤجلة من المالك
- [x] `admin-duty-rosters-page` الإشراف اليومي (ws-timeline حي بخط «الآن» + قوالب الأسبوع)
- [ ] `admin-schedule-simulator-page` المحاكي

### دفعة 8 — الإدارة والأدوات والإعدادات (C + E)
- [ ] `admin-dashboard-page` النظرة العامة (C)
- [ ] `admin-subjects-page`, `admin-forms-page` + تفاصيلها, `admin-import-page`
- [x] `admin-teacher-profile-page` ملف المعلم (قائمة معلمين يمين + بحث مطبَّع + تبويبات seg) — [ ] `admin-team-pulse-page`, `admin-madrasati-report-page`, `admin-teacher-preparation-page`, `admin-lesson-plans-page`, `admin-faris-page`
- [ ] أدوات المدرسة: `admin-school-bell-page`, `admin-auto-call-page`, `admin-academic-calendar-page`, `admin-chat-page`
- [ ] الإعدادات: `admin-settings-page`, `admin-barcode-settings-page`, `admin-barcode-print-page`, `admin-permissions-page`, `admin-theme-settings-page`, `admin-app-notifications-page`, `admin-support-page`, `points-program-page`, `admin-e-store-page`

### الدفعة الختامية — تنظيف الثيم 🧹 (شرط اكتمال المشروع)
- [ ] حذف بلوكات `[data-theme='classic-admin']` و `[data-theme='enterprise-compact']` (مئات أسطر `!important`) من `index.css`
- [ ] تقليص بلوك `[data-theme='dark-green']` إلى overrides الـ tokens فقط
- [ ] حذف `glass-card` من كل صفحات الأدمن
- [ ] مراجعة `theme-definitions.ts`: الثيمات تبقى **ألواناً فقط** تُحقن كمتغيرات — بلا CSS خاص

---

## 5) قواعد إلزامية لكل صفحة تُرحَّل (Checklist)

1. **Tokens فقط**: ممنوع `slate-200` / `teal-600` / hex صلب — كل لون من `--ws-*` أو كلاس `ws-*`.
2. **لا بطاقات KPI في الجسم** — الإحصائيات صف حقائق `ws-header__facts`.
3. أيقونات **lucide-react** فقط (لا bootstrap icons)، خطوط 11–13px.
4. البلوكات مدمجة (`ws-block`) بلا استدارة ولا ظلال؛ البلوك الأخير `--fill` يتمرر داخلياً.
5. طيّ الأعمدة الجانبية بمفتاح `ws:<اسم-الصفحة>:sidecol` في localStorage.
6. **المنطق لا يُمس**: الترحيل شكلي — نفس الـ hooks والـ handlers والسلوك، وأي إصلاح منطقي يكون commit منفصلاً.
7. CSS خاص بصفحة؟ فقط للضرورة القصوى وباسم `ws-<page>-*` داخل workspace.css (قسم مُعلَّم).
8. اختبار قبل الاعتماد: ديسكتوب + جوال (تراص الأعمدة) + ثيم داكن + `npx tsc --noEmit`.

---

## 6) الأداء (يُنفَّذ بالتوازي مع الدفعات)

| الإجراء | لماذا | متى |
|---|---|---|
| **تقسيم الراوتات** `React.lazy` لكل صفحة أدمن | الحزمة الحالية **6.4MB** — تحميل أول بطيء | مع المرحلة 1 (أكبر مكسب أداء في المشروع كله) |
| إبقاء `jspdf`/`html2canvas`/`xlsx` **dynamic import فقط** | تحذير البناء الحالي يشير لاستيرادات ساكنة تسحبها للحزمة الرئيسية | دفعة 2 و7 (صفحات الطباعة/التصدير) |
| `placeholderData: keepPreviousData` في استعلامات الفلاتر | الجدول لا يومض/يقفز عند تغيير فلتر — إحساس «برنامج» | أثناء ترحيل كل صفحة |
| `memo` لصف الجدول + virtualization (`@tanstack/react-virtual`) **فقط** للقوائم > 200 صف | الكثافة العالية = صفوف كثيرة في DOM | عند الحاجة (طلاب/كشوف) |
| ثبات مقاسات الأعمدة والصور | لا layout shift داخل مساحة عمل ثابتة | دائماً |

---

## 7) آلية العمل والاعتماد

```
دفعة → بناء → tsc + build → معاينة منك → تعديلات → اعتماد → commit → الدفعة التالية
```

- كل دفعة commit واحد مستقل برسالة `ui(ws): ترحيل <الدفعة> للنمط الملتصق`.
- العمل على branch الفرونت الحالي `fix/audit-2026-comprehensive` (لا main أبداً).
- أي صفحة لم تُرحَّل بعد تبقى شغالة بشكلها القديم — النظامان يتعايشان حتى نهاية الخطة.
